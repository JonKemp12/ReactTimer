

// Timer page:
var React = require('react');

var Timer = React.createClass({
  render: function() {
    return (
      <div>
        <p>Render Timer component</p>
      </div>
    );
  }
});

module.exports = Timer;
